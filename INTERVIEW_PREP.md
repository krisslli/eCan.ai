# 面试备战手册：电商 AI Agent 架构师 / 核心开发师

> **适用人群**：有 3 年以上多 Agent / RAG / 客服机器人方向经验，以 eCan.ai 为项目载体准备面试。  
> **核心策略**：不背功能列表，而是用**三层讲法**把每个模块讲出深度和设计思维。

---

## 目录

1. [简历模块选择建议](#1-简历模块选择建议)
2. [三层讲法框架](#2-三层讲法框架)
3. [核心模块深度讲解](#3-核心模块深度讲解)
   - [模块 A：多智能体协调系统](#模块-a多智能体协调系统)
   - [模块 B：RAG 知识库架构](#模块-brag-知识库架构)
   - [模块 C：统一对话管理层](#模块-c统一对话管理层)
   - [模块 D：LangGraph 技能工作流](#模块-dlanggraph-技能工作流)
4. [亲历者感强化技巧](#4-亲历者感强化技巧)
5. [被深挖时的应对话术](#5-被深挖时的应对话术)
6. [自我介绍脚本](#6-自我介绍脚本)

---

## 1. 简历模块选择建议

### 推荐写进简历的（与你真实经验高度吻合）

| 模块 | 写法建议 | 为什么推荐 |
|---|---|---|
| **多智能体协调系统** | "设计并实现了基于 A2A 协议的分层多智能体通信架构，支持 LAN/WAN 自动路由" | 与你多 Agent 经验直接对应 |
| **RAG 知识库** | "构建了基于 LightRAG 的图谱增强检索系统，支持向量+图谱双引擎检索和多租户隔离" | 与你 RAG 经验直接对应 |
| **多渠道对话管理** | "实现了统一消息接入层，支持 WhatsApp/Telegram/Slack 等 8+ 渠道，含异步 Human-in-the-loop 交互" | 与你客服机器人经验直接对应 |
| **LangGraph 技能工作流** | "基于 LangGraph 设计了可视化技能编排引擎，支持 12 种节点类型和断点调试" | 补充技术深度，体现架构能力 |

### 不建议主推的（过深容易被追问穿）

- 桌面 GUI（PySide6）——容易被问 Qt 细节
- OTA 更新系统——太专项，与 Agent 岗位关联性弱
- AWS 基础设施细节——除非对方明确要求云架构经验

---

## 2. 三层讲法框架

**面试中"讲不深"的根本原因**：停留在第一层（是什么），没有进入第二层（怎么做）和第三层（为什么这么做）。

```
第一层（30秒）：是什么，解决什么问题
    ↓ 面试官点头 → 继续
第二层（90秒）：具体怎么设计/实现的，有哪些关键机制
    ↓ 面试官追问 → 进入
第三层（60秒）：为什么这么设计，有什么取舍，踩过什么坑
```

**记忆口诀**：**是什么 → 怎么做 → 为什么 → 踩过什么坑**

每次讲一个模块，都按这四步走，永远不会"讲不深"。

---

## 3. 核心模块深度讲解

---

### 模块 A：多智能体协调系统

> 对应代码：`agent/ec_agent.py`、`agent/a2a/`、`agent/ec_skills/ecbot_rpa/`

---

#### 第一层：是什么，解决什么问题（30 秒）

> "我们做的是一个电商场景的多智能体平台，不同的 Agent 负责不同的领域——比如一个 RPA 操作员 Agent 专门操作浏览器，一个客服 Agent 专门处理用户消息，一个调度 Agent 在上层做任务分发。问题是：这些 Agent 运行在不同的机器上，甚至跨网络，怎么让它们高效通信、协同执行，是这个模块要解决的核心问题。"

---

#### 第二层：怎么设计的，关键机制（90 秒）

**层级结构**：

```
Commander Agent（全局调度，分解目标）
       │
  Platoon Agent（领域协调，如 RPA Supervisor）
       │
  Operator Agent（具体执行，如 RPA Operator）
```

**LAN/WAN 双路由**：

每个 Agent 在系统里对应一个 `AgentEndpoint` 数据结构，里面记录两套地址：
- 局域网地址（Zeroconf mDNS 自动发现，`lan_host`、`lan_port`）
- WAN 中继地址（AWS AppSync WebSocket，`wan_relay_channel`）

发消息时，统一走 `unified_send_chat_message()`，它的选路逻辑很简单：如果局域网地址在 180 秒内有心跳，就走直连；否则走 WAN 中继。这样同一套代码，本地部署和云部署都能工作。

**A2A 协议**：

Agent 之间通信用的是标准 A2A 协议，每个 Agent 启动时会暴露一个 HTTP 端点（基于 Starlette），接收其他 Agent 发来的任务请求。任务请求有统一的信封格式，包含 `type`、`source`、`tag` 和 `data`，后续的状态更新也通过同一通道回传。

---

#### 第三层：为什么这么设计，有什么取舍（60 秒）

> "最开始我们想用统一的消息队列（比如 Redis）做中转，但问题是本地部署时不想强依赖额外的中间件——很多用户的电脑上没有 Redis。所以我们选择了 mDNS 局域网发现 + AppSync WAN 中继的组合：局域网内直连，零依赖；跨网络时走云中继，不需要用户手动配置。"

---

#### 踩过的坑（加分项，让讲解有血有肉）

> "有一个坑是 A2A 互发死锁。场景是：Agent A 发消息给 B，同步等 B 确认；B 处理完想回复 A，但 A 的线程被自己的同步等待占住了，B 的回调进不来——两边互等，死锁。"
>
> "解法是非对称设计：A 发给 B 用同步（需要确认 B 收到任务），B 回复 A 用异步 fire-and-forget，提交给独立的线程池，B 的主线程不阻塞。这样彻底消除了互等的可能性。"

---

#### 如果被追问"你们为什么不用 CrewAI/AutoGen？"

> "我们调研过。CrewAI 的 Agent 默认跑在同一进程里，没有真正的跨机器通信；AutoGen 的 GroupChat 是中心化的，不适合我们这种去中心化、跨设备的部署场景。A2A 是 Google 推的开放协议，有完整的规范，我们选择自己实现是因为需要和我们的多渠道消息层做深度集成，现成框架的抽象层太高，接不进去。"

---

### 模块 B：RAG 知识库架构

> 对应代码：`knowledge/lightrag_client.py`、`knowledge/advanced_chunker.py`、`knowledge/lightrag_confidence_scorer.py`

---

#### 第一层：是什么，解决什么问题（30 秒）

> "电商场景的 RAG 有两个特殊挑战：第一，文档类型极其多样——商品规格是表格、售后政策是 Word、价格历史是 Excel，不同格式用同一种分块策略效果很差；第二，多商家共用一套知识库服务，数据不能混，要做租户隔离。我们在 LightRAG 基础上解决了这两个问题。"

---

#### 第二层：怎么设计的，关键机制（90 秒）

**双引擎检索（LightRAG 的核心）**：

LightRAG 同时支持两种检索模式：
- **向量检索**：语义相似匹配，适合"找和这个问题相关的内容"
- **知识图谱检索**：实体关系推理，适合"这个品牌下有哪些产品"、"这个供应商的历史价格趋势"

在电商场景里，这两种往往要结合用——用户问"有没有类似苹果耳机但便宜一半的产品"，需要先语义检索找到"耳机"类目，再通过图谱找到同类目价格更低的商品。

**智能分块路由**：

我们实现了一个 `UniversalTableChunker`，根据文档内容自动判断分块策略：
1. 检测到 Markdown 表格 → 按行分块，保留表头上下文
2. 检测到 Excel sheet → 转成 LightRAG 的结构化摄取格式
3. CSV/TSV → 检测分隔符，按行分块
4. JSON 数组 → 展开每个 object 为独立文档
5. 纯文本 → 标准 token-based 分块

这样不同格式的文档进入同一套 pipeline，最终都能被有效检索。

**多租户隔离**：

每个商家对应一个独立的 LightRAG 工作区，通过 HTTP 请求头 `LIGHTRAG-WORKSPACE` 传递（值用 percent-encoding，支持中文商家名）。服务端根据这个头切换到对应的向量库和图谱实例，数据完全隔离。

**结果可信度评分**：

RAG 结果不是"有没有"，而是"有多相关"。我们加了 `lightrag_confidence_scorer`，对每条检索结果打置信度分，低于阈值的结果不返回给 LLM，避免 LLM 被噪声数据带偏。

---

#### 第三层：为什么这么设计，有什么取舍（60 秒）

> "为什么用 LightRAG 而不是标准的向量数据库（比如 Weaviate/Qdrant）？主要是因为电商场景的商品关系非常复杂，纯向量检索在'关系推理'这类问题上很弱——比如'找同一供应商的所有商品'，向量相似度解决不了这个问题，而知识图谱可以。LightRAG 的代价是摄取速度慢（需要提取实体和关系），所以我们把知识库的更新做成异步批量处理，不阻塞实时查询。"

---

#### 踩过的坑

> "遇到过 embedding provider 的速率限制问题。我们有商家一次性上传了几千个商品文档，触发了 API 的 RPM 限制，整批摄取失败。后来加了 `provider_limits_validator`，在摄取前检查配置的速率限制，自动分批 + 限速，失败了有重试。这是一个做 RAG 生产落地必须考虑的工程问题，POC 阶段往往忽略。"

---

#### 如果被追问"为什么不用 Elasticsearch/BM25？"

> "BM25 是关键词匹配，在电商场景里有明显的问题：用户说'找个防水的运动鞋'，用 BM25 得找 '防水' AND '运动鞋'，但商品描述可能写的是'具有优越防溅性能的跑步装备'，词不同就匹配不到。向量检索解决了语义理解的问题。当然在有精确 SKU ID 查询的场景，我们会在 LightRAG 前加一层关键词过滤，两者结合。"

---

### 模块 C：统一对话管理层

> 对应代码：`agent/chats/`、`agent/channels/`、`agent/ec_skills/ec_customer_support/`、`agent/ec_skill.py`（pend_event_node）

---

#### 第一层：是什么，解决什么问题（30 秒）

> "电商客服需要同时接入多个渠道——WhatsApp、Telegram、微信、邮件、独立站聊天框——每个渠道的消息格式、连接方式、回复限制都不一样。如果在 Agent 逻辑里直接处理这些差异，代码会很混乱而且难以扩展。我们设计了一个统一对话管理层，把所有渠道的消息标准化成统一格式，Agent 只看到一种消息，不关心它从哪个渠道来的。"

---

#### 第二层：怎么设计的，关键机制（90 秒）

**统一事件信封**：

所有进来的消息，无论来自哪个渠道，都被包装成同一种格式：

```json
{
    "type": "human_chat",
    "context": {
        "chatId": "xxx",
        "sessionId": "yyy",
        "senderId": "用户ID",
        "channelType": "whatsapp"
    },
    "data": {
        "human_text": "我想退货",
        "attachments": []
    }
}
```

`type` 字段区分消息来源：`human_chat` 是用户消息，`a2a` 是 Agent 间消息，`timer` 是定时触发，`webhook` 是第三方回调。Agent 的 LangGraph 工作流通过 `pend_event_node` 等待任意一种事件，事件来了就继续执行。

**Human-in-the-loop（人工确认节点）**：

有些操作需要人工确认——比如退款金额超过某个阈值，必须让客服主管审批。`pend_event_node` 就是这个机制的实现：工作流执行到这个节点时暂停，等待特定类型的事件（比如"主管填写了审批表单"），收到事件后继续。这是完全异步的，等待期间 Agent 不占用资源，其他任务可以正常运行。

**状态映射 DSL**：

同一个"用户填的表单"，可能从 WhatsApp 来的路径是 `event.data.form_data`，从网页来的路径是 `event.data.web_form`，但最终都要写入 Agent 状态里的 `state.attributes.forms.customer_form`。

我们用 `mapping_rules` 做声明式的字段映射：
```json
{
    "from": ["event.data.form_data", "event.data.web_form"],
    "to": [{"target": "state.attributes.forms.customer_form"}],
    "on_conflict": "merge_deep"
}
```
`from` 是多源回退（取第一个非空的），`to` 可以同时写多个目标，`on_conflict` 控制合并策略。Agent 代码完全不需要关心数据从哪个渠道来的，统一从 `state.attributes` 里取。

**优先级队列**：

高并发时，用户消息（`HIGH`）和渠道 Agent 间消息（`NORMAL`）优先处理，DOM 事件快照（`LOW`）可以丢弃积压的重复项。

---

#### 第三层：为什么这么设计，有什么取舍（60 秒）

> "为什么选择事件信封这种设计，而不是在每个渠道驱动里直接调用 Agent？主要是为了可测试性和可扩展性。用统一信封，可以在测试里直接构造任意类型的事件注入，不需要真的启动 WhatsApp 或 Telegram。加新渠道时，只需要写一个新的 channel driver，把消息转成信封格式，Agent 侧完全不需要改。"

---

#### 踩过的坑

> "消息积压是一个真实的问题。用户在忙碌时段发了大量消息，Agent 处理速度跟不上，队列越堆越长，最后处理到的是 30 分钟前的消息，回复完全不及时。我们的解法是给 `chat_message` 和 `a2a` 消息加了过期时间（`_STALE_TTL_SEC`），超过阈值的消息直接丢弃，给用户发一个'您好，当前排队人数较多，请稍后'的通知，而不是延迟几十分钟后给出一个过期的回复。"

---

#### 如果被追问"多渠道接入你们用的是什么技术栈？"

> "各渠道用的驱动不一样。WhatsApp 通过 Baileys（Node.js）桥接，我们用 `wabaileys-bridge` 这个子模块做进程间通信；Telegram/Slack 走官方 API，Python SDK；微信/钉钉走企业接口。驱动层只负责接收消息和发送回复，消息格式的标准化在 `UnifiedMessenger` 里统一处理。"

---

### 模块 D：LangGraph 技能工作流

> 对应代码：`agent/ec_skill.py`、`agent/ec_skills/flowgram2langgraph.py`、`agent/ec_skills/build_node.py`

---

#### 第一层：是什么，解决什么问题（30 秒）

> "我们需要让业务人员（不是程序员）也能定义 Agent 的执行逻辑——比如'先查询订单状态，如果是待发货就取消，如果已发货就走退货流程'。直接让他们写 Python 代码不现实，所以我们做了一个可视化的流程图编辑器（Flowgram），用拖拽方式画出工作流，然后在后端把这个流程图编译成 LangGraph 的 StateGraph 来执行。"

---

#### 第二层：怎么设计的，关键机制（90 秒）

**EC_Skill 是核心抽象**：

每个"技能"对应一个 `EC_Skill` 对象，里面核心是三样东西：
1. `diagram`：Flowgram 格式的 JSON 流程图（用户在编辑器里画的）
2. `work_flow`：对应的 LangGraph `StateGraph`（由 diagram 编译而来）
3. `runnable`：`StateGraph.compile()` 后的可执行对象

用户编辑完流程图保存，系统自动把 `diagram` 编译成 `work_flow`，之后执行就是调用 `runnable.invoke()`。

**12 种节点类型**：

编译器（`flowgram2langgraph.py`）维护了一个注册表，把可视化节点类型映射到 Python builder 函数：
- `llm` → 调用 LLM 生成文本
- `condition` → 条件分支（if/else）
- `loop` → 迭代处理列表
- `mcp` → 调用 MCP 工具（查订单、改库存等）
- `browser-automation` → 浏览器操作（截图、点击、输入）
- `pend_event_node` → 等待外部事件（前面提到的人工确认）
- `rag_node` → 查询知识库
- `chat_node` → 向用户发消息

**node_builder 包装层**：

每个节点不是直接跑原始函数，而是经过 `node_builder()` 包装。它做的事是：
- 节点执行前 → 通知前端"这个节点开始运行了"
- 执行失败 → 指数退避重试，最多 3 次
- 断点命中 → 暂停执行，把当前 state 快照发给前端，等用户点"继续"
- 执行完成 → 记录耗时，通知前端节点完成

用户的业务代码完全不知道这些机制存在，`node_builder` 是完全透明的包装。

---

#### 第三层：为什么这么设计，有什么取舍（60 秒）

> "为什么用 LangGraph 而不是自己实现状态机？LangGraph 的 `checkpointer` 机制解决了一个很重要的问题：中断恢复。当 `pend_event_node` 在等待用户确认时，Agent 可以重启（比如服务器重启），恢复后 LangGraph 能从 checkpointer 里还原到中断前的状态，继续等待，用户无感知。这个能力自己实现代价很高。"

> "取舍：LangGraph 的调试体验比较差，执行中间状态不容易观测。这是我们加 `node_builder` 包装层的原因之一——实时把每个节点的状态推给前端，弥补 LangGraph 本身可观测性不足的问题。"

---

#### 踩过的坑

> "LangGraph 有一个坑：Pydantic 模型作为 state 字段时，序列化到 checkpointer 有兼容性问题（特别是 ormsgpack 和 Pydantic v2 的递归深度限制）。我们在启动时打了一个 patch，把 LangGraph 的 checkpoint 序列化器替换掉，这是一个比较隐蔽的生产问题，不跑长流程（有大量嵌套 state）不会触发。"

---

## 4. 亲历者感强化技巧

讲解时加入这些元素，听起来像是真正做过的人：

### 技巧一：用"我们当时"开头，描述决策过程

❌ "系统用了 LightRAG 做 RAG"（像在读文档）

✅ "我们当时对比了几个方案——Weaviate、Qdrant 纯向量、Elasticsearch、LightRAG——最后选 LightRAG 是因为……"（像是亲历决策的人）

---

### 技巧二：讲"反面教材"——你们踩过的坑

每个模块准备 1 个具体的踩坑故事（上面每个模块都有），讲坑比讲成功更有说服力，因为只有真做过的人才会踩到那些坑。

---

### 技巧三：引用具体数字或参数

❌ "重试的等待时间是指数增长的"

✅ "重试等待是 `base_delay * 2^attempts + random.jitter`，base_delay 我们设的是 1 秒，最多 3 次，第三次失败后标记为 failed 状态推给前端"

具体的参数让你的描述更可信，也体现你真的看过代码。

---

### 技巧四：主动提局限性

❌ 把一切都描述得完美无缺

✅ "fire-and-forget 的问题是不确认对方收到没有，我们目前靠 AppSync 持久化兜底，如果要做高可靠场景，应该加确认 + 重传机制，这是已知的 limitation"

主动说出系统的不足，是真正做过的人才会有的姿态。

---

### 技巧五：讲"这和业界常见做法的区别"

❌ "我们实现了 A2A 通信"

✅ "大多数 Agent 框架（CrewAI、AutoGen）的 Agent 跑在同一进程里，我们的场景是 Agent 跑在不同的机器上，所以需要真正的网络通信层，这是我们选择实现 A2A 协议而不是用现成框架的原因"

---

## 5. 被深挖时的应对话术

### 场景一：被问到你不熟悉的技术细节

> "这块我当时主要负责 XX 层，底层的 YY 实现细节是另一位同学负责的，我能从设计角度讲清楚为什么这么做，但具体的实现细节我可能记不太准了。你如果对这块感兴趣，我可以讲讲我们当时做这个设计决策时考虑过哪些方向……"

---

### 场景二：被问"你们团队几个人？分工是什么？"

> "核心团队大概 4~5 人，我主要负责 Agent 编排层和知识库这两块，前端可视化编辑器是另一个同学做的，基础设施（AWS、Docker 部署）是运维同学配合的。我可以深入讲的是我自己做的这部分，其他模块我大概知道思路，但细节不一定都记得。"

---

### 场景三：被追问"这个设计是你想的，还是参考了什么？"

> "两者都有。比如 `pend_event_node` 这个等待外部事件的机制，是我们在用 LangGraph 时自然遇到的需求——客服场景里经常需要等人工确认，LangGraph 的 `interrupt()` 正好提供了这个能力，我们是在它上面封装了一层更业务化的接口。A2A 协议本身是 Google 推的开放规范，我们实现了它。"

---

### 场景四：被问"项目上线了吗？有多少用户？"

> "我们做的是一个平台级的产品，主要面向中小电商商家，核心功能已经跑在真实的商家环境里了，服务了一批独立站商家和平台商家。具体的数据（MAU、GMV）因为 NDA 我不方便透露，但可以说规模在 [你知道的范围内合理描述]。"

---

### 场景五：被问"代码能给我看看吗？"

> "这个项目的代码目前还没有公开，因为里面有一些商业敏感逻辑。我可以给你画出核心模块的架构图，或者讲某个具体实现的伪代码，你觉得哪种方式更方便？"

（如果面试官坚持，可以准备一个把敏感内容删掉后的 demo 版本的核心模块伪代码。）

---

## 6. 自我介绍脚本

> 总时长约 3 分钟，三段结构，口语化，不要逐字背。

---

### 第一段：定位（30 秒）

"我在 AI Agent 和对话 AI 方向做了 3 年多，主要聚焦三个方向：多智能体系统的设计和编排、RAG 知识库的工程落地，以及电商场景的客服机器人。"

---

### 第二段：项目（2 分钟）

"我最近做的是一个面向电商商家的 AI Agent 平台，核心是让商家能快速搭建自己的 AI 智能体，用来自动化客服、RPA 操作、商品上架等场景。

我负责的核心部分有三块：

**第一块是多智能体协调。** 我们做了一个三层的 Agent 架构——调度层、协调层、执行层——Agent 之间通过 A2A 协议通信，局域网内走 mDNS 直连，跨网络走 AppSync 中继，统一接口自动选路。遇到过 Agent 互发消息死锁的问题，用非对称的发送策略解决——发起方同步，回复方异步 fire-and-forget。

**第二块是 RAG 知识库。** 用的是 LightRAG，支持向量检索和知识图谱两种模式，电商场景的关系推理（比如'同一品牌的所有产品'）纯向量解决不了，图谱补上了这块。文档分块做了智能路由，表格、Excel、纯文本各走不同策略。多租户通过 HTTP 头的工作区隔离实现，每个商家数据完全独立。

**第三块是统一对话管理。** 接入了 8+ 个渠道，WhatsApp、Telegram、微信等，用统一事件信封标准化消息格式，Agent 不关心消息从哪来。加了 Human-in-the-loop 的审批节点，退款这类高风险操作必须人工确认才能继续，整个流程是异步的，等待期间不阻塞其他任务。"

---

### 第三段：收尾（30 秒）

"我对这个方向的理解是：电商 AI Agent 不只是'接入一个 LLM'，核心是把 LLM 和具体的业务流程、数据来源、审批机制结合起来，让它在真实的业务环境里可靠地工作。这也是我来这里的原因，我希望把这些积累用在更大的规模和更复杂的业务场景上。"

---

## 附录：复习顺序建议

面试前 1 天，按以下顺序复习，不超过 3 小时：

1. **先把 4 个模块的"三层讲法"各过一遍**，确保每层都能流畅说出（不需要背词，理解结构）
2. **练自我介绍**：对着计时器讲 2~3 遍，控制在 3 分钟内
3. **把"踩过的坑"的 4 个故事各讲一遍**，这是最有力的加分项
4. **看一眼关键代码**，加深印象（不需要背代码，能说出大概结构即可）

**关键代码复习路径（按重要性）：**
- `agent/ec_agent.py` — Agent 类结构、`_init_a2a_server()`、`a2a_send_chat_message_async()`
- `agent/ec_skill.py:48-154` — `mapping_rules` DSL 定义
- `knowledge/lightrag_client.py` — `_ws_headers()`（多租户隔离）、`ingest_files()`
- `knowledge/advanced_chunker.py` — 分块路由逻辑
- `agent/ec_skills/build_node.py:19-53` — `function_registry` 节点类型映射

---

*基于 eCan.ai 代码库分析生成，结合多 Agent / RAG / 客服机器人方向 3 年以上经验定制。*
